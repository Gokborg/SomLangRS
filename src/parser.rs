use std::collections::HashMap;

struct TokenBuffer {
    pos: usize,
    content: Vec<Token>,
    current: Token,
    done: bool
}

impl TokenBuffer {
    
}

struct Parser {

}

impl Parser {
    pub fn parse(&mut self, tokens: Vec<Token>) {
        
    }
}